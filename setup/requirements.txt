# Séjour Pour Tous — Python dependencies
# Tuesday smoke test needs ONLY the ollama client. The rest land on Wed/Thu.

ollama==0.4.7                     # Tue: offline generation + tool-calling client

# --- Wed (data + retrieval) ---
# sentence-transformers==3.3.1    # offline multilingual embeddings (bge-m3 / MiniLM)
# sqlite-vec==0.1.6               # local vector store with SQL as-of prefilter
# beautifulsoup4==4.12.3          # scrape Legifrance / service-public
# httpx==0.28.1                   # fetch source pages during prep (online, prep-time only)

# --- Thu/Fri (MCP + UI) ---
# fastmcp==2.10.0                 # expose query_immigration_law as an MCP tool
# fastapi==0.115.6                # minimal demo UI backend
# uvicorn==0.34.0                 # serve the UI
