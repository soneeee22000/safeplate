# SafePlate — runtime dependencies only. Development tools: requirements-dev.txt.
# The model runs in a local Ollama daemon, reached over its HTTP API with the
# standard library, so no model client is installed here. Audio is transcoded by
# an ffmpeg binary on PATH when one is present.

fastapi==0.139.2         # orchestrator HTTP surface
uvicorn==0.51.0          # ASGI server
python-multipart==0.0.32 # form fields and audio uploads on POST /api/case
