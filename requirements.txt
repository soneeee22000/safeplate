# SafePlate — dependencies
# The model and OCR run locally; only the product lookup leaves the machine.

ollama==0.4.7            # local Gemma 4 client (generation, tool-calling)
pytesseract==0.3.13      # wrapper around the local Tesseract binary
pillow==12.3.0           # image loading for label photos

# --- orchestrator ---
fastapi==0.139.2         # agent loop HTTP surface
uvicorn==0.51.0          # server
python-multipart         # label photo upload

# --- external evidence ---
requests==2.32.3         # SerpApi product / allergen lookup

# --- dev ---
pytest==9.1.1
