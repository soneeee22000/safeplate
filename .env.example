# SerpApi — manufacturer allergen declarations.
# Get the key from https://serpapi.com/manage-api-key and copy this file to .env
# Free plan is 250 searches/month; every response is cached to fixtures/serp/,
# so the demo runs with the network off.
SERPAPI_KEY=
